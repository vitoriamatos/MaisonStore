import React from 'react'
import { PaymentFormContainer, Button } from './styles'

const Payment = () => {
  return (
    <PaymentFormContainer>
      <Button>Ir para o pagamento</Button>
    </PaymentFormContainer>
  )
}

export default Payment
